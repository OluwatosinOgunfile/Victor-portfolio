"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export function EnquiryActions({id,status}:{id:string;status:string}){const router=useRouter();const[busy,setBusy]=useState(false);const[body,setBody]=useState("");const[message,setMessage]=useState("");const call=async(payload:object)=>{setBusy(true);setMessage("");try{const response=await fetch(`/api/admin/enquiries/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.error||"Update failed");setMessage("Saved successfully");router.refresh();return true}catch(error){setMessage(error instanceof Error?error.message:"Update failed");return false}finally{setBusy(false)}};return <><div className="detail-actions"><select value={status} disabled={busy} onChange={e=>call({action:"status",status:e.target.value})}>{["New","Contacted","Qualified","Won","Closed"].map(x=><option key={x}>{x}</option>)}</select><button disabled={busy} onClick={()=>call({action:"retry"})}>{busy?"Working…":"Retry notification"}</button></div><form className="note-form" onSubmit={async e=>{e.preventDefault();if(!body.trim())return;if(await call({action:"note",body}))setBody("")}}><textarea rows={4} value={body} onChange={e=>setBody(e.target.value)} placeholder="Add a private note…"/><button disabled={busy}>{busy?"Saving…":"Add note"}</button></form>{message&&<p className="action-message" role="status">{message}</p>}</>}
