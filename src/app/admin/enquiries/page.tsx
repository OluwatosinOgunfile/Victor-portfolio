import { AdminShell } from "../layout";
import { requireAdmin } from "@/lib/admin";
import { createServiceClient } from "@/lib/supabase-server";
import { Filter, Inbox, Search } from "lucide-react";
import Link from "next/link";

export default async function EnquiriesPage({searchParams}:{searchParams:Promise<{q?:string;status?:string}>}){await requireAdmin();const{q="",status=""}=await searchParams;const db=createServiceClient();let query=db.from("enquiries").select("*").order("created_at",{ascending:false});if(q)query=query.or(`name.ilike.%${q.replace(/[%_,]/g,"")}%,email.ilike.%${q.replace(/[%_,]/g,"")}%,company.ilike.%${q.replace(/[%_,]/g,"")}%`);if(status)query=query.eq("status",status);const{data=[]}=await query;const{count:unread=0}=await db.from("enquiries").select("id",{count:"exact",head:true}).eq("is_read",false);return <AdminShell unread={unread||0}><header className="admin-header"><div><p className="admin-kicker">LEAD PIPELINE</p><h1>Project enquiries</h1><p>Review, qualify and manage every opportunity.</p></div><div className="header-stat"><span>Unread leads</span><strong>{unread||0}</strong></div></header><form className="admin-filters"><div className="search-field"><Search/><input name="q" defaultValue={q} placeholder="Search name, email or company"/></div><div className="select-field"><Filter/><select name="status" defaultValue={status}><option value="">All statuses</option>{["New","Contacted","Qualified","Won","Closed"].map(x=><option key={x}>{x}</option>)}</select></div><button className="admin-button">Apply filters</button></form><div className="admin-card table-card enquiries-card"><div className="card-heading"><div><p className="admin-kicker">INBOX</p><h2>{data?.length||0} {data?.length===1?"enquiry":"enquiries"}</h2></div></div><div className="table-scroll"><table className="admin-table"><thead><tr><th>Lead</th><th>Service</th><th>Country</th><th>Received</th><th>Status</th></tr></thead><tbody>{(data||[]).map(e=><tr key={e.id} className={!e.is_read?"unread-row":""}><td data-label="Lead"><Link href={`/admin/enquiries/${e.id}`}><span className="lead-avatar">{e.name.slice(0,2).toUpperCase()}</span><span><b>{!e.is_read&&<i className="unread-dot"/>}{e.name}</b><small>{e.email}</small></span></Link></td><td data-label="Service">{e.service}</td><td data-label="Country">{e.country||"Unknown"}</td><td data-label="Received">{new Date(e.created_at).toLocaleDateString("en-GB",{day:"2-digit",month:"short",year:"numeric"})}</td><td data-label="Status"><span className={`status status-${String(e.status).toLowerCase()}`}>{e.status}</span></td></tr>)}{!data?.length&&<tr><td colSpan={5}><div className="empty-state"><Inbox/><b>No matching enquiries</b><span>Try changing your search or status filter.</span></div></td></tr>}</tbody></table></div></div></AdminShell>}
